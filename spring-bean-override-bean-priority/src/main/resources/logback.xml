<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Spring Bean 工厂相关日志 -->
    <!-- 设置为 INFO 级别，Bean 覆盖的 DEBUG 日志将不会输出（静默覆盖） -->
    <logger name="org.springframework.beans.factory.support.DefaultListableBeanFactory" level="INFO"/>
    <logger name="org.springframework.beans.factory.support" level="INFO"/>
    <logger name="org.springframework.beans.factory" level="INFO"/>
    <logger name="org.springframework.context" level="INFO"/>
    
    <!-- 保留我们自定义的 AppConfig 日志 -->
    <logger name="com.tt.config.AppConfig" level="INFO"/>
    
    <!-- 根日志级别设置为 INFO -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
